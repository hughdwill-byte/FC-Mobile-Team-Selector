# Pinned to well-supported, wheel-available versions so `pip install` just works
# on Windows/macOS/Linux with no compiler needed.
fastapi>=0.110,<1.0
uvicorn>=0.27,<1.0
sqlmodel>=0.0.16,<0.1
scipy>=1.11,<2.0
numpy>=1.26,<3.0
python-multipart>=0.0.9,<1.0
