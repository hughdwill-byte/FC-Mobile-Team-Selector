# Minimal, wheel-available dependencies. No database driver needed - the app uses
# Python's built-in sqlite3. No Node.js toolchain required anywhere.
fastapi>=0.110,<1.0
uvicorn>=0.27,<1.0
scipy>=1.11,<2.0
numpy>=1.26,<3.0
python-multipart>=0.0.9,<1.0
