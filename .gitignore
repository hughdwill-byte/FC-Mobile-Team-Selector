# Local database (back it up by copying it, don't commit it)
/data/
*.sqlite3
*.sqlite3-journal

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
.pytest_cache/
*.egg-info/

# OS
.DS_Store
Thumbs.db

# Raw card spreadsheet (has image URLs); only the image-free docs/data/cards.json is published
*.xlsx
